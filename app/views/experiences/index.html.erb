<p id="notice"><%= notice %></p>

<h1>Experiences</h1>

<table class="table">
    <thead>
        <tr>
            <td>Nom</td>
            <td></td>
            <td></td>
            <td></td>
        </tr>
    </thead>

    <tbody>
        <% @experiences.each do |experience| %>
        <tr>
            <td><%= experience.name %></td>
            <td><%= link_to 'Afficher', experience, class: 'btn btn-warning' %></td>
            <td><%= link_to 'Editer', edit_experience_path(experience), class: 'btn btn-info' %></td>
            <td><%= link_to 'Supprimer', experience, method: :delete, remote: true, class: 'btn btn-danger' %></td>
        </tr>
        <% end %>
    </tbody>
</table>

<br>

<%= link_to 'Nouvelle expérience', new_experience_path, class: 'btn btn-success' %>
