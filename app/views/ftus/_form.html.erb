<%= simple_form_for(@ftu) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
      <%= f.input :annee %>
      <%= f.input :etablissement %>
      <%= f.input :description %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
