<p id="notice"><%= notice %></p>

<h1>Etus</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @etus.each do |etu| %>
      <tr>
        <td><%= link_to 'Show', etu %></td>
        <td><%= link_to 'Edit', edit_etu_path(etu) %></td>
        <td><%= link_to 'Destroy', etu, method: :delete, remote: true %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Etu', new_etu_path %>
