<p id="notice"><%= notice %></p>

<h1>Offres</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @offres.each do |offre| %>
      <tr>
        <td><%= link_to 'Afficher', offre, class: 'btn btn-warning' %></td>
        <% if current_user.role == 'Admin' %>
        <td><%= link_to 'Editer', edit_offre_path(offre), class: 'btn btn-info' %></td>
        <td><%= link_to 'Supprimer', offre, method: :delete, data: { confirm: 'Etes vous sûr ?' }, class: 'btn btn-danger' %></td><% end %>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nouvelle Offre', new_offre_path, class: 'btn btn-success' %>
