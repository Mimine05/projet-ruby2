<h1>Les différents profils</h1>

<table>
    <thead>
        <tr>
            <th colspan="3"></th>
        </tr>
    </thead>

    <tbody>
        <% @users.each do |user| %>
        <tr>
            <td><%= user.username %></td>
            <td><%= link_to 'Show', {controller: "users", action: "profil", user_id: user.id} %></td>
        </tr>
        <% end %>
    </tbody>
</table>