<p id="notice"><%= notice %></p>

<h1>Offres</h1>

<table class="table">
  <thead>
    <tr>
      <th>Description<th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @otes.each do |ote| %>
      <tr>
        <td><%= ote.description %></td>
        <td><%= link_to 'Show', ote, class: 'btn btn-warning' %></td>
        <% if current_user.role == 'Admin' %>
        <td><%= link_to 'Edit', edit_ote_path(ote), class: 'btn btn-info' %></td>
        <td><%= link_to 'Destroy', ote, method: :delete, data: { confirm: 'Etes vous sûr ?' }, class: 'btn btn-danger' %></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nouvelle association', new_ote_path, class: 'btn btn-success' %>
